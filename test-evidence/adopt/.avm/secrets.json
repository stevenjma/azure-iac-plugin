{
  "pass": "3.4-secrets",
  "status": "complete",
  "secrets_identified": [
    {
      "resource": "Microsoft.KeyVault/vaults/secrets",
      "name": "demo-secret",
      "module": "keyvault",
      "handling": "value sourced from sensitive variable kv_secret_demo_value; imported by versioned data-plane id"
    }
  ],
  "sensitive_vars_declared": ["kv_secret_demo_value"],
  "literal_secrets_removed": true,
  "keyvault_wired": true,
  "example_tfvars_file": "secrets.auto.tfvars.example",
  "runtime_tfvars_file": "secrets.auto.tfvars",
  "notes": "Real secret value lives only in untracked secrets.auto.tfvars (gitignored). Committed tree keeps only the .example placeholder. Value never printed to logs; wired to module secrets_value map so the imported secret matches live with no diff."
}
