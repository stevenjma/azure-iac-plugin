# Copy this file to secrets.auto.tfvars (which must remain untracked) and fill in
# the real secret value before running `terraform plan`. Never commit the
# populated file.
kv_secret_demo_value = "REPLACE_WITH_DEMO_SECRET_VALUE"
