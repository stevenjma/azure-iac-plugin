{
  "mcpServers": {
    "arm-mcp": {
      "type": "http",
      "url": "${ARM_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${ARM_MCP_TOKEN}"
      },
      "tools": ["*"]
    }
  }
}
